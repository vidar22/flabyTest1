.features { 
    position: relative;
    overflow: hidden;
}

/* Make correct margins for some items */
.features .half-content-r { margin-top: 135px; margin-bottom: 180px; }
.features ul { margin-top: 50px; margin-bottom: 50px; }

/*#region Background*/
/*
    Work with back ground as always
*/
.features .background {
    /* Stretch background to width and height of parent*/
    width: 100%;
    height: 100%;
    /* Don't touch other elements */
    position: absolute;
    /* Be behind the body*/
    z-index: -1;
    /* Set background image */
    background-image: url('../img/2_features/shape.png');
}

/* Container of iphone and ipad will be have half of parent */
.features .background .img-wrapper-flex {
    width: 50%;
    height: 100%;
    display: flex;
    /* Centerize vertically */
    align-items: center;
    /* Set position of elements to and*/
    justify-content: end;
}

/* Container with ipad and iphone, that will have ipad size */
.features .background .img-wrapper {
    position: relative;
    margin-right: 140px;
}

.features .background .img-wrapper .img-ipad {
    /* Set margin for iphone nice visibility */
    margin-right: 75px;
    /* Beautify */
    filter: drop-shadow(0px 50px 50px rgba(0, 0, 0, .2));
}

.features .background .img-wrapper .img-iphone {
    /* Dock iphone image tp right bottom */
    position: absolute;
    right: 0px;
    bottom: 4px;
    /* Beautify */
    filter: drop-shadow(-10px 2px 4px rgba(0, 0, 0, .2));
}

/*#endregion*/
